info:
  name: Ask
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3000/chat/ask
  body:
    type: json
    data: |-
      {
        "question": "Need to info about orderId #ORD-1001 tracking id what"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
