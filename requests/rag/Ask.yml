info:
  name: Ask
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3000/chat/ask
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
